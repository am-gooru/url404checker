{
  "db.username": "nucleus",
  "db.password": "nucleus",
  "db.url": "jdbc:postgresql://localhost:5432/nucleus",
  "batch.size": 1000,
  "thread.pool.size": 5,
  "interval.between.jobs.seconds": 600,
  "http.timeout.seconds": 30,
  "db.pool.size": 8
}